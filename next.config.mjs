// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['maps.google.com'],
    },
    // other configurations...
};

export default nextConfig;
